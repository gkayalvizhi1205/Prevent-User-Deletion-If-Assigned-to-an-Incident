# Prevent-User-Deletion-If-Assigned-to-an-Incident
This functionality prevents the deletion of a user record if that user is currently assigned to any incident. It ensures that important incident records are not affected by accidental deletion of users.
